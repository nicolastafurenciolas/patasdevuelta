# Sistema
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/

# Variables de entorno y secretos (NUNCA subir claves service_role o de Resend)
.env
.env.local
.env.*.local

# Dependencias (si en algún momento se usa npm para algo puntual)
node_modules/

# Lista de las publicaciones de prueba sembradas: lleva los tokens de gestión,
# que son enlaces de administración sin contraseña. No van al repositorio.
pruebas/ruido-sembrado.json

# fichas de demostracion: llevan tokens de gestion
pruebas/demo-sembrado.json

pruebas/vigilancia-estado.json
